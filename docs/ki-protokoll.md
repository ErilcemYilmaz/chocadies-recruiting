# KI-Protokoll

Laufendes Protokoll der Claude-Nutzung fuer die APDE-Transferaufgabe
(Grundlage fuer die KI-Deklaration vom 29.08.2026). Bitte ab jedem
Arbeitsblock ergaenzen, nicht nachtraeglich rekonstruieren.

| Datum | Werkzeug | Zweck / Prompt (Kurzfassung) | Ergebnis / Uebernahme |
|---|---|---|---|
| 2026-08-18 | Claude (Cowork) | MongoDB-Setup: Ordnerstruktur des Repos angelegt, Mongoose-Schema und Data-Access-Layer fuer das Datenobjekt Bewerbung erstellt (passend zu api/openapi.yaml und architektur_komponenten.puml), Verbindungs-Testskript geschrieben, Anleitung fuer MongoDB Atlas M0 (Region Frankfurt) erstellt. | Code in server/src (config, models, repositories) und scripts/testConnection.ts vollstaendig uebernommen, vor Verwendung durchgelesen und Feldnamen gegen openapi.yaml geprueft. |
| 2026-08-18 | Claude (Cowork) | Fehlersuche beim ersten Verbindungstest (fehlende .env, danach bad auth). | Ursachen selbst identifiziert und behoben: .env.txt zu .env umbenannt, Datenbankbenutzer-Passwort in Atlas neu gesetzt. Claude hat keine Zugangsdaten gesehen oder eingegeben. Verbindungstest danach erfolgreich (Ping-Antwort { ok: 1 }). |
| 2026-08-24 | Claude (Cowork) | Kapitel 4.2 (Server-App): vollstaendige Web-API-Schicht implementieren (Controller, Routen, JWT-Authentisierung mit Rollen intern/personalvermittlung, Zod-Validierung, zentrale Fehlerbehandlung), passend zu api/openapi.yaml und dem Zugriffsschutz-Konzept aus Kapitel 4.1. Ausserdem toolbasierte API-Tests aufbauen. | Code in server/src (errors, middleware, schemas, controllers, routes, app.ts, index.ts) sowie server/scripts/generateTestToken.ts vollstaendig durchgelesen, gegen openapi.yaml geprueft und uebernommen. Selbst verifiziert statt nur generiert: 16 Vitest/Supertest-Tests (server/tests/api.test.ts, Auth/Validierung/Zugriffsschutz/Fehlercodes) und ein automatisierter Newman-Lauf der echten Postman-Collection (tests/chocadies-recruiting.postman_collection.json, server/tests/newman-smoke.test.ts) gegen die Basis-Applikation ausgefuehrt, 12/12 Requests und 19/19 Assertions erfolgreich; TypeScript-Build fehlerfrei. Eine Abnahme gegen die echte Atlas-Datenbank steht noch aus und muss von Hand nachgeholt werden (kein MongoDB-Zugriff in der Cloud-Sandbox moeglich). |
| 2026-09-23 | Claude Code (VS Code) | Repo-Stand analysiert, uncommittete Arbeit vom 24.08. in thematische Commits aufgeteilt und gepusht, server/.env.example aus der Versionierung genommen, Arbeitsteilung Claude Code (Code) / Claude-Projekt (Fliesstext) eingerichtet: CLAUDE.md, Uebergabe-Vorlage und erste Uebergabe fuer Kapitel 4.2. Newman-Test fuer Windows korrigiert (npx ueber Shell starten). | Commit-Aufteilung und Push von mir freigegeben. Tests lokal selbst ausgefuehrt: 16/16 Vitest, Newman-Lauf gruen, Build fehlerfrei. Hinweis von Claude uebernommen, dass PUT den Status nicht aendern kann, Entscheidung dazu noch offen. |
| 2026-09-23 | Claude Code (VS Code) | Luecke behoben, dass der Status einer Bewerbung per PUT nicht aenderbar war: OpenAPI-Schema BewerbungAenderung (status nur fuer Rolle intern, sonst 403), Statusverlauf im Mongoose-Modell, Anpassung von Zod-Schema, Controller, Repository, Unit-Tests und Postman-Request 08. Abnahme der API gegen die echte MongoDB-Atlas-Datenbank per Newman. | Umsetzung von mir in Auftrag gegeben und uebernommen. Spezifikation mit Redocly CLI validiert. Tests: 20/20 Vitest, Newman gegen Atlas 12/12 Requests und 20/20 Assertions, Statusverlauf direkt in Atlas geprueft. Claude hat die lokale .env genutzt, um den Server zu starten, ohne Zugangsdaten auszugeben. |
